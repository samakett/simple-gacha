# simple-gacha

A simple gacha-pull system made with Cursor.

## Run locally

Open `index.html` directly in a browser, or serve the workspace with:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
